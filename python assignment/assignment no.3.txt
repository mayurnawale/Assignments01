1.
  Functions reduce the need for duplicate code. it makes programs shorter, easy to read  and easy to update. 
 
2.
  when it's called.
 
3.
  The “def” keyword is a statement for defining a function in Python, start a function with the def keyword, specify a name followed by a colon (:).

4.
  A function is to achieve particular result whereas function call is used to achieve that task.

5.
  Global scope=one
  Local scope=four

6.
 The assignment creates variables in the local scope,so the assignment inside the function does not modify the global variable.
 it creates a new local variable called a , and assigns the value 3 to that variable.

7.
 A return statement is used to end the execution of the function call and “returns” the result (value of the expression following the return keyword) to the caller.
 The statements after the return statements are not executed. If the return statement is without any expression, then the special value None is returned.

8.
  If no return statement appears in a function definition, control automatically returns to the calling function after the last statement of the called function is executed.
 In this case, the return value of the called function is undefined.
9.
  Need to use the global keyword to declare which variables are global.
10.
  The None keyword is used to define a null variable, None keyword is an object.
11.
  That import statement imports a module named areallyourpetsnamederic. 
12.
  This function can be called with spam.

13.
  error handling can be used to notify the user of why the error occurred and gracefully exit the process that caused the error.

14.
  In the try clause, all statements are executed until an exception is encountered and except is used to catch and handle the exceptions that are encountered in the try clause.