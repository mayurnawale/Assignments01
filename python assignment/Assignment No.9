1)
  A relative file path is interpreted from the perspective your current working directory. If you use a relative file path from the wrong directory, then the path will refer to a different file or it will refer to no file at all.
  
2)
  An absolute path refers to the complete details needed to locate a file or folder, starting from the root element and ending with the other subdirectories.
  
3)
  os.getcwd():it returns the absolute path of the working directory.
 os.chdir():it is used to change the current working directory to specified path.
  
4)
  These are . (current directory) and .. (parent directory).
  
5)
  C:\bacon\eggs is the dir name, while spam. txt is the base name.
  
6)
  r : Opens the file in read-only mode.
rb : Opens the file as read-only in binary format and starts reading from the beginning of the file.
r+ : Opens a file for reading and writing, placing the pointer at the beginning of the file.

7)
  If a file with the same name already exists,its contents are discarded and the file is treated as a new empty file.
  
8)
  read() would treat each character in the file separately, means that the iteration would happen for every character,whereas the readline() function on the other hand reads only a single line of the file.
  
9)
  A shelf value resembles a dictionary value. it has keys and values, along with keys() and values() methods that work similarly to the dictionary methods of the same names.