1)
  addition
  subtraction
  multiplication
  division

2)
  Operator Overloading means giving extended meaning beyond their predefined operational meaning and it is done in the context of overriding the method.

3)
  at the start create slice object then an integer number specifying start index.It is optional and default is 0 after that an integer number specifying end index, then an integer number specifying the step of slicing.It is optional but default value is 1.

4)
  in-place addition function is used to assign and add the current value. This operation does “a+=b” operation. Assigning is not performed in case of immutable containers, such as strings, numbers and tuples.

5)
  when we making a new class that falls into an existing abstract base class then it is useful to use operator overloading. 