1)
  The try and except block in Python is used to catch and handle exceptions.Python executes code the try statement as a normal part of the program. The code that follows the except statement is the program's response to any exceptions in the preceding try clause.

2)
  Syntax errors and Exceptions.

3)
  raise allows you to throw an exception at any time,the Python interpreter raises an exception each time it detects an error in an expression or statement,we can also raise exceptions with raise and assert statements.

4)
  An assert statement checks whether a condition is true. If a condition evaluates to True a program will keep running and if a condition is false then the program will return an AssertionError and at this point the program will stop executing.

5)
  The with statement in Python is used for resource management and exception handling.it most likely find it when working with file streams.